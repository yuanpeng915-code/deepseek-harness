/** Host loader entry for the model-board browser implementation exported from `./client`. */

/** Host plugin body — no host-side behavior for the model pricing board. */
export function apply(): void {}
