/** Host loader entry for the file-tree browser implementation exported from `./client`. */

/** Host plugin body — no host-side behavior for the file-tree browser. */
export function apply(): void {}
